# AI Voice Assistant – Python test pipeline
# Cài đặt: pip install -r requirements.txt
# sounddevice + numpy thay cho pyaudio (cài dễ hơn trên Windows)

sounddevice>=0.5.0
numpy>=1.26.0
groq>=0.11.0
requests>=2.32.0
python-dotenv>=1.0.1
edge-tts>=6.1.0

# Yêu cầu hệ thống: ffmpeg trong PATH (chứa ffplay dùng cho playback)
# Windows: winget install ffmpeg   |   choco install ffmpeg
# Mac    : brew install ffmpeg
# Linux  : sudo apt install ffmpeg
#
# Không dùng pydub/audioop/pyaudioop nữa vì Python 3.13+ đã bỏ stdlib audioop.
# Chuông báo tạo từ numpy + stdlib `wave`; playback qua ffplay subprocess.
